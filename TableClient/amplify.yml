version: 1
frontend:
  phases:
    preBuild:
      commands:
        # - nvm install 20
        - nvm use 18
        # - cd TableClient
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: TableClient/dist
    files:
      - '**/*'
  cache:
    paths:
      - TableClient/node_modules/**/*
