version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 20
        - nvm use 20
        - cd TableClient
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: TableClient/dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
